const list=document.getElementById('mfoList');const skeleton=[...document.querySelectorAll('.mfo-card.skeleton')];let visible=0;const step=10;const show=()=>{for(let i=visible;i<Math.min(visible+step,skeleton.length);i++){skeleton[i].classList.remove('skeleton');skeleton[i].innerHTML='<div class="mfo-card__title">Карточка загружена</div><p>Контент подгружен по событию.</p>';}visible=Math.min(visible+step,skeleton.length);} ;const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting&&e.target.classList.contains('skeleton')){e.target.classList.remove('skeleton');e.target.innerHTML='<div class="mfo-card__title">Карточка загружена</div><p>Подгрузка IntersectionObserver.</p>';io.unobserve(e.target)}}),{rootMargin:'120px'});skeleton.forEach((n,i)=>{if(i<10)io.observe(n)});document.getElementById('showMoreBtn')?.addEventListener('click',show);

const sum=document.getElementById('sumRange');const days=document.getElementById('daysRange');const sumOut=document.getElementById('sumOut');const over=document.getElementById('overpayOut');const total=document.getElementById('totalOut');const rate=0.1;const fmt=n=>new Intl.NumberFormat('ru-RU').format(Math.round(n));const calc=()=>{if(!sum||!days)return;const s=+sum.value,d=+days.value;const p=s*(rate/100)*d/365;sumOut.textContent=`${fmt(s)} ₽`;over.textContent=`${fmt(p)} ₽`;total.textContent=`${fmt(s+p)} ₽`;};sum?.addEventListener('input',calc);days?.addEventListener('input',calc);calc();

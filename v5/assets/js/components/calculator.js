const sum=document.getElementById('sumRange');const days=document.getElementById('daysRange');const sumOut=document.getElementById('sumOut');const over=document.getElementById('overpayOut');const total=document.getElementById('totalOut');const dailyRate=0.01;const fmt=n=>new Intl.NumberFormat('ru-RU').format(Math.round(n));const calc=()=>{if(!sum||!days)return;const s=+sum.value,d=+days.value;const p=s*dailyRate*d;sumOut.textContent=`${fmt(s)} ₽`;over.textContent=`${fmt(p)} ₽`;total.textContent=`${fmt(s+p)} ₽`;};sum?.addEventListener('input',calc);days?.addEventListener('input',calc);calc();

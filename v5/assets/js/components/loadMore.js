const placeholders=[...document.querySelectorAll('.js-lazy-card, .mfo-card.skeleton')];let visible=0;const step=10;const render=(node)=>{node.classList.remove('skeleton','js-lazy-card');node.innerHTML='<div class="skeleton-placeholder">Загрузка...</div>';};const show=()=>{for(let i=visible;i<Math.min(visible+step,placeholders.length);i++){render(placeholders[i]);}visible=Math.min(visible+step,placeholders.length);};const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){render(e.target);io.unobserve(e.target)}}),{rootMargin:'120px'});placeholders.forEach((n,i)=>{if(i<10)io.observe(n)});document.getElementById('showMoreBtn')?.addEventListener('click',show);
